# RCC builds this env first; the robot task then uses `uv run` + pyproject.toml / uv.lock.
# https://github.com/robocorp/rcc/blob/master/docs/recipes.md#what-is-in-condayaml

channels:
  - conda-forge

dependencies:
  - python=3.12.13
  - uv=0.10.9
